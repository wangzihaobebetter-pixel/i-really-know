import { Placeholder } from '../../app/Placeholder';
export default function CohortScreen() {
  return <Placeholder screen="Cohort" owner="WP7" does="Import submissions, generate a probe set per student, read the class heatmap, export printable viva scripts and student tickets." />;
}
