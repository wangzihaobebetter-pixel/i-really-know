import { Placeholder } from '../../app/Placeholder';
export default function SettingsScreen() {
  return <Placeholder screen="Settings" owner="WP8" does="Your provider, your key, your model — stored only in this browser. Appearance, language, data export and wipe." />;
}
