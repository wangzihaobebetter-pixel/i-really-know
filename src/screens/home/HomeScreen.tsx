import { Placeholder } from '../../app/Placeholder';
export default function HomeScreen() {
  return <Placeholder screen="Verify" owner="WP4" does="Paste or drop your own finished work, confirm the detected discipline, pick a run preset, and start the viva. The sample gallery runs with no key at all." />;
}
