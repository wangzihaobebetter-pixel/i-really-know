import { Placeholder } from '../../app/Placeholder';
export default function VivaScreen() {
  return <Placeholder screen="Viva" owner="WP4" does="One probe at a time, timed, no going back. Commit your answer, self-grade it, then — and only then — see how it actually scored." />;
}
