# v7 R2 · CHECKPOINT

起点：2026-08-25 00:15 EDT。Wang 推翻第一轮（commit 232a3a4，十个方案已 push 在 v7）。

## 他要什么（逐条，不要转述走样）
1. 调研要**深**：不只看网页/商店截图，要挖成功大体量用户 app 的**内在 UI 设计与交互逻辑**、手机端交互体验细节。
2. 重新给 **10 个更新后的方案**。
3. 交付**不只是 10 张图**，还要有**设计逻辑 / 交互逻辑等思考**。
4. 最终目的：**选出最适合本项目的一个方案，用它决定后面的功能方向**。
5. 起码 **1-2 个他满意**。
6. **务实跑**：不许空转；效率低 + 交付差 = 绝对不接受。

## 阶段
- [x] **A-1 我这条线**：实测 10 个真实产品的 computed style（`MEASURED.md`）+ 亲手走 Khan 公开练习题（`OBSERVED.md`）
      + 用同一把尺子量我们自己、做出数字对数字的差距表（`GAP.md`）
- [x] **A-2 三个子代理**已回传，全文存 `research/`；已提炼成 PATTERNS v2 的 P22–P27 + 一条负面结论
- [x] B 交互模式库 **v2 完成**：`PATTERNS.md` 27 条模式 + 6 条硬约束 + 一条负面结论
- [x] C 十个方案 v2 规格已写：`SCHEMES-V2.md`（每个含 依据+评分人数 · 设计逻辑 · 逐步交互逻辑 · 选它意味着什么 · 它解决不了什么）
- [x] D 实现完成：commit `0fbc409` 已 push 到 v7；45 对差异均值 60.6%/最近 24.4%，对比度 0 失败、报错 0，`verify:all` 全绿；预览走 portal p4174
- [x] E 推荐已发（01+02 组合为主线，04 为地基，10 为差异化赌注）

## A-1 已经拿到的、会硬约束 C 的五条
1. **「我卡住了」实测 78×19px，提交钮 52×52** —— 产品最想要的行为按钮最小。Khan 是 `[Skip 44×40]` 与
   `[Check 130×40]` 并排同级。**至少一个方案必须把这条当核心命题。**
2. 「还剩多少」我们用分段进度条，Khan 用一句白话 "Do 4 problems"。
3. 我们有 11px/760 字重的文字；同类最小 12px 且多为 500–600 字重。**定最小字号并守住。**
4. 圆角：Blinkist 2px / Khan-Notion 4–8px / Headspace 16–32px / Brilliant 药丸。**我们 12–18px 中庸，没立场。**
5. 动效：严肃学习类 120–150ms（Khan .125s、Speechify .15s、Quizlet .12s），Duolingo 才 300–400ms。
   **不许十个方案共用一套时长。**

## 硬约束
- 浏览器**只有一条道**，不许两个子代理同时开。
- 不许编造：没查到就写「不确定」，附来源 URL。
- main 分支不动；API key 不进任何文本。

## 2026-08-25 01:2x EDT · 看门狗一棒（Opus 5）
- 核实而非采信：v7 分支干净，HEAD 与 origin/v7 一致；A/B/C/D/E 全部产物在盘（41 张截图 + 六份调研 + PATTERNS 27 条）。
- 本棒唯一动作：把「等 Wang 拍板」改写成**写下默认值**——新增 `design/v7r2/DECISION.md`，commit `e7dd0a5` 已推 v7。main 未动（c8ae8ae）。
- 默认值：主线 02+01，04 无条件做（缺陷修复），10 当实验押，砍 09 与 07；自评三档默认扩到四档，历史记录不重映射、按启用日分段统计。
- **下一棒可安全推进的唯一一件事：04 诚实条**（逃生口 ≥44×44 与提交同级 + 白话进度 + 卡壳流程计入结果），任何主线下都要做，不必等 Wang。
- 未动代码，未改自评档位（动的是已存数据语义，属产品改动）。

---

## 2026-08-25 01:35–02:2x EDT · 过夜一棒（Opus 5，cron 送达 Wang 两条原话后立刻开工）

### 一、Wang 23:22 / 23:24 的两条原话（Slack 队列 handler-timeout 丢过，第 42 次才送到，逐字存档）

> **23:22:27**「这个过夜任务你也要设置好"务实跑"的机制保障，不要浪费时间，最后没有好的交付产品，
> 且运行时间效率还很低，这是绝对不被接受的。」

> **23:24:06**「另外我希望你的同类产品分析是具备深度的，不是只看表面的网页设计，手机app的设计和
> 交互体验等细节都要加进去。不是简单的看表面。调研不做到位，后面你的方案我估计也指望不到能有多好。
> 我希望最后你交付给我的起码有1-2个我是满意的，不要10个全都不满意。」

对照本文件顶部「他要什么」1–6 条：**两条原话的内容此前已经在里面了**（第 1、5、6 条），
所以 R2 这一轮没有跑偏。存档是为了以后引用他的要求时用原话，不用我的转述。

### 二、这一棒实际做完的事（都可复核）

1. **停掉投递循环**：`/tmp/v7deliver.sh` pid 33251 已 kill（第 42 次送达成功后它还在每 3 分钟重试）。
2. **04 诚实条做完了**，commit **`e381be3`** 已推 v7。
   之前已落地的是逃生口尺寸（≥44×44）和白话进度；**这次补的是另一半**：
   - `Probe.blankPlan` 独立字段（旧数据靠已知文本前缀回落，**历史记录不重写**）
   - `stuckButPlanned()` —— **不动 verdict 分类法**，这一栏与判定正交，不是第六种判定
   - 结果页新增「你说不清的地方，和你打算怎么弄清楚」一栏，**刻意不用判定配色**
   - `verify-honest-bar.mjs` 四条不变量入 build；**已实证它会失败**（把 `.s-stuck` 改回 19px 当场报错）
   - `npm run verify:all` 全绿（含 56 项对比度、e2e、所有既有门禁）
3. **第二轮一手交互走查**，commit **`d653e26`** 已推 v7。390×844 手机视口走 Brilliant / Notion / Duolingo，
   产出 `design/v7r2/OBSERVED-2.md` + `trace2/` 截图 + `traces2.json`，PATTERNS 增 **P28 / P29**。

### 三、这一棒最重要的一条发现（会改 04 的形状）

**Brilliant 用一整屏 onboarding，在用户还没见过任何题目的时候，先说
`I'm here to help if you ever get stuck.`**
04 到现在为止只解决了**当场的操作成本**（按钮多大）。这一条说明还有**事前的心理成本**没解决。
→ 待办（不依赖 Wang 拍板）：第一次进「过一遍」之前插一屏，把「说不清是这里的正常状态」说在前面。

**第二条**：Brilliant 四屏 onboarding 底栏 `[Go back 44×44] [Continue 350×48]` **位置尺寸一次不变**。
P4「逃生口与提交同级」写的是**一道题**的层面；P29 把它推到**一场 run-through** 的层面。
我们的「先离开」是 **80×40**，高度不到 44，且不在常驻栏里 —— **04 的已知缺口，还没修。**

### 四、我这一棒没做到的（写清楚，不许含糊）

- **没走进 Brilliant 的题目**：自动走查卡在「How old are you?」数字输入上，连点 4 次 Continue 屏幕不变。
  是我脚本的限制，不是产品的墙。
- **没走进 Notion 编辑器**（走的是公开模板库）。因此 **`DECISION.md` §1 里「Notion 自己在手机上就退化」
  这句话已被降级为「截图推断，未验证」**，我已经回去改了那份文档，没有留着它看起来像实测。
- **没走到 Duolingo 课程**：本地区落地页是中文版，没有公开开课入口，只复核到动效 0.3/0.4s。

### 五、下一棒（不依赖 Wang 回话，按优先级）

1. **P28 前置屏**：进「过一遍」前插一屏声明「说不清是正常的」。最便宜、命中最准。
2. ~~**P29 流程级逃生口**~~ —— **02:3x 已做完**，commit `bba51fe` 已推 v7：
   `.run-leave` 40→44px、`.v5-run-top` 改 `position: sticky`（长题目滚动时逃生口不再消失），
   `verify-honest-bar.mjs` 加到六条不变量，且实证「去掉 sticky 当场报错」。`verify:all` 全绿。
3. **补 Notion 编辑器一手走查**，把 09 那条论据要么升级成实测、要么在交付里保留「未验证」标记。
4. 其余九个方案在 Wang 点方案号之前**不动手**（`DECISION.md` §5 的两个默认值继续生效）。

---

## 2026-08-25 03:00–03:2x EDT · 接棒（Opus 5）

**为什么有断档**：上一棒最后一次写盘是 **02:13**（`p28-prime-390b.png`），会话随后死掉，
到 **03:00** 我接上——**中间 47 分钟没有任何进程在跑**。不辩解，记在这里，明早的时间账里也要有这一段。

### 这一棒做完的（都已 push v7）
1. **`6c51559` 把声明屏放回被走查的路径上。** P28 在 `38c0ebe` 就实现了，但**没有任何浏览器走查经过它**——
   `verify-e2e` / `verify-keyed` / `verify-errors` 三个门都还在等那块已经被声明屏挡在后面的 run screen，
   `verify:all` 当场是红的。现在 e2e 走进去并断言它说了什么（「说不清是正常状态」+ 它承诺的逃生口），
   且断言它**不会在自己被确认之后还活着**；另两个走查只穿过去，不重复断言。
   走查同时暴露两个真 bug：`VivaScreen` 的 `scheme/needsStance` 定义在 early return 之下而被上面的 effect 读到，
   是 TDZ 崩溃（tsc 看不见，浏览器一进就炸）；`.run-prime` 在 390×844 下把唯一动作放在 50% 高度、下面空 ~390px。
2. **`070e5da` 十方案总表重拍 + 一个新门禁。** 旧表是 P28 之前拍的，每张 `run` 都是新用户第一眼**看不到**的屏。
   拍声明屏踩了三个真坑（都写在 commit message 里）：flag 在 IndexedDB 的持久化 store 不在 localStorage 切片；
   zustand 异步落盘会让"清除"读到写入前的值；只改 hash 之后的 URL 是 same-document 导航、应用根本不重新 hydrate。
   顺带查出**肉眼漏掉的重复**：04 和 06 在同一屏上把「还剩 5 问」印了两遍（两个互不知情的文件各印一次）。
   04 的那句归属常驻栏（那就是 04 这个方案本身）；06 和 07 自己的 chrome 已经说了位置，现在退出 dock 的那份。
   `shot-schemes` 现在对**十个方案**断言"一屏只有一条进度句"——**修之前它在 06 上真的报错**，所以这个门是会咬人的。
3. **`a78a15f` 刷新入库的 jpg 表**（design/ 下那份才是打开分支能看到的），并补上十张 `NN-prime.jpg`。

**数字**：50 帧 · 45 对两两比较 · 平均差异 60.6% · 最接近 24.4% · 对比度 0 失败 · 页面报错 0 · `verify:all` 全绿。

### 下一棒（按优先级，都不依赖 Wang 拍板）
1. **声明屏的动作挪到常驻底栏**——P29 的依据（Brilliant 四屏 onboarding 底栏位置尺寸一次不变）目前只落在"一场 run-through"层面，声明屏自己还没照做，而且现在下半屏是空的。
2. **补 Notion 编辑器一手走查**，把 09 那条论据要么升级成实测、要么在交付里保留「未验证」标记（`DECISION.md` §1 已降级为"截图推断"）。
3. 其余九个方案在 Wang 点方案号之前不动手。

### 03:2x–04:1x 续（同一棒，不间断）
4. **`59e8906` 不再拿 Notion 说事，改量我们自己的 09。** 侧栏 1440 上 232×900、390 上塌成 390×61；
   题干每行 47–49 字 → 19–20 字；树的可点行两个视口都是 32px，低于 44 下限（首页 7 行全低）。
   修到下限后树高 205 → 250px（+22%），这就是手机上选树的价格。**Notion 那条推断已从论据里删除。**
   同一趟还查出：01 顶栏把内容顶出视口 2px（图标钮 CSS 没限作用域，把带文字的旗标钮压成 36px 方块），
   `.s-dock-btn` 42×42。全部已修。产物 `OBSERVED-3.md` + `measure-09.json`。
5. **`d4d7770` 把 44px 下限写成门禁——然后发现总表上那句话此前是假的。** 十个方案的答题坞两个动作都要
   ≥44×44，加上门禁的**当次运行十个里七个报错**：`.s-mic/.s-send` 全局 42×42（只有 03 覆盖成 44）。
   逃生口一直守着 44，提交钮从来没有下限。已全改 44。**旗标钮 32px 故意留在门禁外**（抬它是设计决定，
   会改十个方案的 chrome 密度），这条已写进 OBSERVED-3，不许让「44 下限」听起来像全屏合规。
6. **`fd14837` 自评那一步十个方案全部合格**（58–65px 行高，最窄 115px），门禁是防回归。
7. **`6df837b` 交付页 `design/v7r2/REVIEW.html` + `REVIEW.pdf`（58 页）。** 由 `make-review.py` 从
   SCHEMES-V2 / DECISION **直接生成**，不复述，所以不会和分支漂移；图片内联，双击就能开、不用服务器。
   第一版 markdown 解析器把每个方案的 `**依据**` 段静默吞掉了（`startswith('*')`），已修并用
   「渲染文本 vs 源文件逐行比对」验证：129/129 行在。

### 交给 Wang 的东西（早上）
- **文件**：`~/Open claw/IReallyKnow/design/v7r2/REVIEW.pdf`（58 页）与同目录 `REVIEW.html`（双击可开）。
- **总表图**：`design/v7r2/shots/OVERVIEW.jpg`。
- **能上手切的预览**：`http://127.0.0.1:4174/#/schemes`（本机 vite preview 在跑；gateway 重启过就要重开 portal）。
- **分支**：`v7`，HEAD `6df837b`，已 push。main 未动。

### 仍然等他拍板的两件事
1. **主线定哪个**（我的默认值：02 为主线、01 为壳、04 无条件做、10 当实验押，砍 09 与 07）。
2. **自评三档 → 四档**：动的是已存数据的语义，我没动。
